services:
  talentflow-candidate:
    build: .
    container_name: talentflow-candidate
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    stdin_open: true
    ports:
      - "5000"
    tty: true
    networks:
      - agents-net
networks:
  agents-net:
    external: true
